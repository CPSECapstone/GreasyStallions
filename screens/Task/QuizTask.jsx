import React from 'react';
import { View } from 'react-native';
import { Form, Button, FormLabel, ButtonGroup, ToggleButton } from 'react-bootstrap';

let QuizTask = function({ navigation }) {

   return (
      
   );
}

export default QuizTask;